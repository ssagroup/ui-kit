import styled from '@emotion/styled';

const NavBarList = styled.ul`
  display: flex;
  flex-wrap: wrap;

  list-style: none;

  align-items: center;
  align-self: center;
  height: 486px;

  padding: 0;
`;

export default NavBarList;
