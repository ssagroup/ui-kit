/** Smallest width a column can be dragged to, in pixels. */
export const DEFAULT_MIN_COLUMN_WIDTH = 64;

/** Width change per arrow-key press on a resize handle, in pixels. */
export const KEYBOARD_RESIZE_STEP = 8;

/** Width change per Shift + arrow-key press on a resize handle, in pixels. */
export const KEYBOARD_RESIZE_STEP_LARGE = 40;

/** Hit area of the drag handle, inset into the right edge of the cell. */
export const RESIZE_HANDLE_WIDTH = 8;
