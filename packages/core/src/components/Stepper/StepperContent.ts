import styled from '@emotion/styled';

export const StepperContent = styled.div`
  span {
    font-weight: 400;
    font-size: 0.6rem;
  }

  h6 {
    font-weight: 500;
    font-size: 0.6rem;

    margin: 0;
  }
`;
