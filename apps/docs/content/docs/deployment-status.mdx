---
title: Deployment Status
description: Confirms which commit this docs site was built from, so you can verify a new deploy actually went out.
---

This page is baked at **build time**, so the values below reflect whichever
commit Vercel most recently built and deployed — reload this page after a
deploy to confirm it picked up your latest changes.

<DeploymentInfo />

If "Environment" reads `local`, you're viewing a local dev server rather than
a Vercel deployment — Vercel populates these fields automatically for every
build via its [System Environment Variables](https://vercel.com/docs/projects/environment-variables/system-environment-variables),
no extra configuration required.
